<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internal storage -->
    <files-path
        name="internal_files"
        path="." />

    <!-- External storage for shared attachments -->
    <external-files-path
        name="attachments"
        path="." />

    <!-- Optionally: images from camera -->
    <cache-path
        name="cached_images"
        path="." />

</paths>
